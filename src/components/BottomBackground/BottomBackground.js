import React from 'react';

import styles from './BottomBackground.module.css';

const BottomBackground = () => {
  return (
    <div className={styles.bottomBackground}>
    </div>
  );
};

export default BottomBackground;
